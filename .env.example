# Só necessário se o backend PHP estiver noutro domínio/subdomínio do frontend.
# VITE_API_BASE_URL=https://api.exemplo.com

# Opcional: ativa o botão "Sign in with Google" na página de login.
# Tem de corresponder ao GOOGLE_CLIENT_ID configurado no backend (backend/.env).
# VITE_GOOGLE_CLIENT_ID=changeme.apps.googleusercontent.com
